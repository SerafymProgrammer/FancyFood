import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  date_time_touchables: {
    paddingVertical: 10,
    paddingHorizontal: 20,
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: 20,
    marginTop: 20,
    marginHorizontal: 20,
    borderWidth: 1,
    borderColor: 'black',
    borderRadius: 12,
  },
});

export default styles;
